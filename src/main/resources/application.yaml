spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hotel
    username: javadmin
    password: semeru

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect